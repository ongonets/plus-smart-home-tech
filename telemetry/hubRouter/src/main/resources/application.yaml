spring:
  application:
    name: hubRouter
  config:
    import: configserver:http://localhost:8080
  cloud:
    config:
      fail-fast: true
      retry:
        useRandomPolicy: true
        max-interval: 6000